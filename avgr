#include<iostream>
using namespace std;
int main(){

  int size,sum=0;
  int ary1[]={1,2,3,4,5,6};
  float avg;
  for(int i=0;i<6;i++){
    sum+=ary1[i];
  }
size=sizeof(ary1)/sizeof(ary1[0]);
  avg=sum/size;
  cout<<"the avg of the array is : "<<avg; 
    return 0;
}
